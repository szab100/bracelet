<menu xmlns:android="http://schemas.android.com/apk/res/android">
	<item android:id="@+id/measurelist_refresh"
	   	  android:icon="@drawable/refresh"
          android:title="@string/measurelist_menu_refresh" />
                   
    <item android:id="@+id/measureslist_back"
    	  android:icon="@drawable/back"
          android:title="@string/measurelist_menu_back" />
</menu>